nav(class="navbar navbar-expand-lg navbar-dark bg-dark")
  a.navbar-brand(href="/") dlux
  .collapse.navbar-collapse
    .navbar-nav
      a.nav-item.nav-link(href="/2d/feed/dlux") 2D Trending
      a.nav-item.nav-link(href="/feed/dlux") VR Trending
    .navbar-nav.ml-auto
      if user
        if user.name = user
          a.nav-item.nav-link(href=`/@${user}`) VR Profile
          a.nav-item.nav-link(href=`/2d/@${user}`) 2D Profile
          a.nav-item.nav-link(href="/auth/logout") Logout
        else
          a.btn.btn-primary(href="/auth") Login With SteemConnect
