section.container.profile__subnav
  .row
    a.btn.btn-dark(href=`/@${username}`) Blog
    a.btn.btn-dark(href=`/@${username}/transfers`) Transfers
    if user
      if user.name == user
        a.btn.btn-dark(href=`/@${username}/feed`) Your Feed
